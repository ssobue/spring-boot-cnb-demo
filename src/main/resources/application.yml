spring:
  application:
    name: CloudNativeBuildpackDemo
  main:
    banner-mode: off
  profiles:
    active: dev

logging:
  level:
    org.apache.tomcat: warn
    org.apache.catalina: warn
    org.apache.coyote: warn
    org.springframework: warn
